<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.djamil.suntelecom.librairies">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".App"
        tools:replace="android:theme"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.Librairies">

        <activity
            android:name=".MainActivity2"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".takephoto.MainActivity"
            android:exported="true"/>
        <activity
            android:name=".sample_easy_rv_ind.MainActivity3"
            android:exported="true"/>

        <activity
            android:name=".viewflipperwithindicator.MyActivity"
            android:exported="true"/>

        <activity
            android:name=".FormulaireActivity"
            android:exported="true"/>

        <activity
            android:name=".SampleActivity"
            android:exported="false"/>
    </application>

</manifest>